name: linkrot
version: '2.2'
summary: linkrot
description: |
  This script searches a pdf for hyperlinks and tests them for active status or errors returned.
base: core20

parts:
  linkrot:
    plugin: python
